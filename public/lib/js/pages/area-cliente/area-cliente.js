// --------------------------------------------// ------------------ LOGIN -------------------// --------------------------------------------// --------- AÑADIR ESTILO AL HACER CLICK SOBRE BOTON-ENVIAR CONTACTO -----------function validarLogin() {    window.setTimeout(function capturaCambiadatos() {        let letEncabezadoLogin = $("#encabezadoLogin");        let letSubEncabezadoLogin = $("#subEncabezadoLogin");        let letErrorLogin = $("#errorLogin");        if ($(letErrorLogin).css('display') === 'block') {            $(letErrorLogin).css("opacity", "0");            window.setTimeout(function () {                $(letErrorLogin).css("opacity", "100");            }, 300);        } else {            $(letEncabezadoLogin).css("display", "none");            $(letSubEncabezadoLogin).css("display", "none");            $(letErrorLogin).css("display", "block");        }    }, 1500);}// ----- ENVIAR FORMULARIO SIN RECARGAR PÁGINA MEDIANTE AJAX ------function enviarLogin() {    // Capturamos todos los valores    let usuarioLogin = document.getElementById('idUsuarioLogin').value;    let passLogin = document.getElementById('idPasswordLogin').value;    // Hacemos una nueva httpRequest    let xhttp = new XMLHttpRequest();    //Cuando el xhttp esté listo    xhttp.onreadystatechange = function () {        // Si hay 5 tipos de xhttp.readyState. El 1 no se ha enviado, el 2 que se recibió,  el 3 se está procesando, el 4 se procesó y ya se recibieron los datos.        // Status solo hay dos. 200 que se procesó toro bien y el 404 que es error.        if (xhttp.readyState === 4 && xhttp.status === 200) {            // El elemento que tenga el id (formularioContacto), lo va a refrescar y a poner a cero.            //document.getElementById("formularioContacto").innerHTML = xhttp.responseText;        }    };    // El método post es más robusto el get es más rápido    // Llama al fichero login.php y asíncrono (true).    xhttp.open("POST", previoLocalHostJavaScript + "/lib/php/login.php", true);    // El header es tipo de encabezado. Content-type es el tipo de contenido.    xhttp.setRequestHeader("Content-type", "application/x-www-form-urlencoded");    // El nombre entre comillas es el nombre que se va a agarrar en $_POST en php. El nombre entre signos más es el valor del var de más arriba.    xhttp.send("reciboUsuario=" + usuarioLogin + "&reciboPassLogin=" + passLogin + "");    // Limpiar el formulario    document.getElementById("formularioAcceso").reset();    validarLogin();}// --------------------------------------------// -------------- RESTAURAR PASS --------------// --------------------------------------------// --// -- CAMBIA EL CONTENEDOR AL PULSAR SOBRE OLVIDADO CONTRASEÑA// --$(document).ready(function () {    $("#linkOlvidadoPass").click(function mostrarRestablecerContrasenia(e) {        let letFormularioAcceso = $("#idContenedorFormularioAcceso");        let letFormularioRestablecer = $("#idContenedorFormularioRestablecer");        $(letFormularioAcceso).css("opacity", "0");        $(letFormularioRestablecer).css("opacity", "0");        window.setTimeout(function () {            $(letFormularioAcceso).css("display", "none");            $(letFormularioRestablecer).css("display", "block");            window.setTimeout(function () {                $(letFormularioRestablecer).css("opacity", "100");            }, 500);        }, 500);    });});// --// -- MODIFICA EL TEXTO. GRACIAS, NOS PONDREMOS EN CONTACTO// --$(document).ready(function () {    $("#idBotonRestaurarPass").click(function mostrarCambiatextoRestaurar(e) {        let letCabeceraRestaurar = $("#encabezadoLoginRestaurar");        let letsubCabeceraRestaurar = $("#subEncabezadoLoginExplicacion");        let letTextoGracias = $("#textoRestaurar");        $(letCabeceraRestaurar).css("opacity", "0");        $(letsubCabeceraRestaurar).css("opacity", "0");        $(letTextoGracias).css("opacity", "0");        window.setTimeout(function () {            $(letCabeceraRestaurar).css("display", "none");            $(letsubCabeceraRestaurar).css("display", "none");            $(letTextoGracias).css("display", "block");            window.setTimeout(function () {                $(letTextoGracias).css("opacity", "100");            }, 500);        }, 500);    });});function enviarRestaurarPass() {}